# Hermes Browser Bridge — 隐私政策

**最后更新：2026-05-13**

---

## 概述

Hermes Browser Bridge（以下简称"本扩展"）是一个本地浏览器桥接工具，旨在让 Hermes AI Agent 通过本地 WebSocket 连接控制浏览器。**本扩展不收集、不存储、不传输任何用户个人数据。**

---

## 数据收集

本扩展 **绝不收集** 以下任何信息：

- ❌ 个人身份信息（姓名、邮箱、电话等）
- ❌ 浏览历史或访问记录
- ❌ Cookie 或登录凭据
- ❌ 键盘输入或表单内容（仅在用户明确通过 Hermes Agent 指令发起操作时临时处理）
- ❌ 设备信息或系统配置
- ❌ IP 地址或网络位置
- ❌ 使用统计或行为分析

---

## 数据传输

本扩展的 WebSocket 通信 **仅连接本地地址（127.0.0.1）**，具体如下：

| 连接目标 | 协议 | 端口 | 说明 |
|---------|------|------|------|
| `127.0.0.1` | WebSocket | 8643 | 连接本地 Hermes Bridge Server |
| `127.0.0.1` | HTTP | 8642 | 接收本地 Hermes Agent 指令 |

**所有数据传输均在您的本地计算机上完成，绝不经过任何外部服务器或第三方服务。**

---

## 权限说明

本扩展申请的 Chrome 权限及使用目的：

| 权限 | 用途 | 数据访问范围 |
|------|------|------------|
| `activeTab` | 获取当前活动标签页的截图 | 仅限于用户明确指令下的当前标签页 |
| `tabs` | 导航到指定 URL、查询标签页状态 | URL 信息仅在本地处理，不传输 |
| `scripting` | 注入内容脚本以操作页面 DOM | 仅执行用户指令指定的操作 |
| `storage` | 保存桥接服务器地址配置 | 仅存储用户手动输入的 IP 和端口号 |
| `<all_urls>` | 允许在任意页面执行内容脚本 | 仅限于用户指令指定的页面操作 |

---

## 第三方服务

本扩展 **不集成任何第三方服务、分析工具、广告平台或外部 API**。

---

## 数据保留

- 本扩展不存储任何用户数据
- `storage` 权限仅用于保存桥接服务器的连接配置（IP 地址和端口号）
- 这些配置数据仅存储在您的本地 Chrome 配置中，不会被读取或传输

---

## 儿童隐私

本扩展不面向 13 岁以下儿童，也不收集任何年龄段的个人身份信息。

---

## 变更

隐私政策的任何变更将在本页面更新。由于本扩展不收集数据，实质性变更的可能性极低。

---

## 联系

如有关于隐私的疑问，可通过以下方式联系：

- 项目地址：https://github.com/nousresearch/hermes-agent
- 或通过 Hermes Agent 项目提交 Issue

---

**总结：这是一个纯本地的浏览器桥接工具。零数据收集，零外部传输，零第三方服务。**
